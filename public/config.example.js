// Plantilla de config.js — NO se usa directamente.
// En local: copiá este archivo a config.js y completá los valores.
// En Vercel/Netlify: config.js se genera solo en cada build a partir de las
// variables de entorno SUPABASE_URL y SUPABASE_ANON_KEY (ver scripts/generate-config.js
// y README.md) — no hace falta tocar este archivo para deployar.
window.IGNITE_BRIEF_CONFIG = {
  SUPABASE_URL: "https://TU-PROYECTO.supabase.co",
  SUPABASE_ANON_KEY: "TU-CLAVE-ANON-PUBLICA",
};
